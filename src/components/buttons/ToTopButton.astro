---
import { Icon } from "astro-icon/components";
---

<script type="module">
    const backToTop = document.getElementById("return-to-top");

    window.addEventListener("scroll", function (e) {
        if (window.scrollY > 40) {
            backToTop.style.opacity = "1";
            backToTop.style.display = "flex";
        } else {
            backToTop.style.display = "none";
        }
    });

    backToTop.addEventListener("click", () => {
        scrollToTop(500);
    });

    function scrollToTop(scrollDuration) {
        var scrollStep = -window.scrollY / (scrollDuration / 15),
            scrollInterval = setInterval(function () {
                if (window.scrollY !== 0) {
                    window.scrollBy(0, scrollStep);
                } else clearInterval(scrollInterval);
            }, 15);
    }
</script>

<a id="return-to-top" class="nav-up-arrow fixed bottom-8 right-8 w-24 h-24 flex justify-center items-center text-decoration-none rounded-full transition-all duration-300 ease-in-out opacity-0 hover:opacity-50 hover:scale-110" href="#top" aria-label="Return to top">
    <Icon name={"arrow-up-circle"} width={32} height={32} />
</a>